machine:
    environment:
        PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
    node:
        version: 7

dependencies:
    override:
        - yarn
    cache_directories:
        - ~/.cache/yarn

test:
    override:
        - npm run lint && npm run test:coveralls
